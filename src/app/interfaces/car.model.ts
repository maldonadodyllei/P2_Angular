export interface Car {
    brand: string;
    model: string;
    category: string;
    image: string;
    cv: number;
    maxSpeed: number;
    accelerationTime: number;
    price: number;
    available: boolean;
}
